<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.fatihhernn.onboardingviewpager">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.OnBoardingViewPager">
        <activity android:name=".activities.MainActivity2"></activity>
        <activity android:name=".activities.HomeActivity" />
        <activity android:name=".activities.OnBoardingFinishActivity" />
        <activity
            android:name=".activities.MainActivity"
            android:theme="@style/Theme.OnBoardingViewPager.fullscreen">
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>